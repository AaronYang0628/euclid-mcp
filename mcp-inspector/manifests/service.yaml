apiVersion: v1
kind: Service
metadata:
  name: mcp-inspector
  labels:
    app: mcp-inspector
spec:
  type: ClusterIP
  ports:
    - port: 6274
      targetPort: 6274
      protocol: TCP
      name: web
    - port: 6277
      targetPort: 6277
      protocol: TCP
      name: mcp
  selector:
    app: mcp-inspector
