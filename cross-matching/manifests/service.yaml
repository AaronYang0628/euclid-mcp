apiVersion: v1
kind: Service
metadata:
  name: euclid-crossmatch-mcp
  labels:
    app: euclid-crossmatch-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: euclid-crossmatch-mcp
