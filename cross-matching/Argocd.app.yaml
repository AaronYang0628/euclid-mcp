kubectl -n argocd apply -f - <<EOF
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: euclid-crossmatch-mcp
spec:
  project: default
  source:
    repoURL: 'https://gitlab.zhejianglab.com/AaronYang/euclid-mcp.git'
    targetRevision: main
    path: cross-matching/manifests
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=false
  destination:
    server: https://kubernetes.default.svc
    namespace: mcp
EOF
