apiVersion: v1
kind: Service
metadata:
  name: euclid-catalog-mcp
  labels:
    app: euclid-catalog-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: euclid-catalog-mcp
