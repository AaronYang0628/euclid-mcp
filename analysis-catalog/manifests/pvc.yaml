apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: euclid-catalog-pvc
  labels:
    app: euclid-catalog-mcp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: local-path
